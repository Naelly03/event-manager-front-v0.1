# Event Manager (Frontend)

Interface moderna desenvolvida para a gestão completa de eventos, desde a criação até ao controle de inscrições e convites. Este projeto foi construído utilizando Next.js com a nova arquitetura (App Router).

## 🛠 Tecnologias

* Next.js
* Typescript
* Tailwind

## Funcionalidades Principais

* **🔐 Autenticação:** Páginas de Login (`/login`) e Cadastro (`/cadastro`).
* **📊 Dashboard:** Painel de controle do usuário (`/dashboard`).
* **🎉 Eventos:** Criação de novos eventos (`/criareventos`).
* **📝 Inscrições:** Gestão de participações (`/inscricoes`).
* **📩 Convites:** Sistema de convites (`/convites`).
* **🗺️ Mapas:** Integração com mapas (`/map`).

## ▶️ Como rodar o projeto

1. **Clone o repositório e entre na pasta:**
   ```
   git clone [https://github.com/naelly03/event-manager-front-v0.1.git](https://github.com/naelly03/event-manager-front-v0.1.git)
   cd event-manager-front-v0.1
2. Instale as dependências: Recomendado usar pnpm devido ao arquivo de lock existente
   ```
   pnpm install
3. Execute o servidor de desenvolvimento:
  ```
  pnpm run dev
  ```
4. Acesse no navegador: Abra http://localhost:3000 para ver o resultado.

## 📂 Estrutura de Pastas

* `src/app`
* `public`
* `src/componentes`
